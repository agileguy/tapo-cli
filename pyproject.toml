[project]
name = "tapo-cli"
version = "0.0.1"
description = "Deterministic local-LAN CLI for TP-Link Tapo cameras and doorbells"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11,<4.0"
authors = [{ name = "Dan Elliott" }]
classifiers = [
  "Development Status :: 1 - Planning",
  "Environment :: Console",
  "Intended Audience :: System Administrators",
  "License :: OSI Approved :: MIT License",
  "Operating System :: MacOS",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Home Automation",
  "Topic :: Multimedia :: Video :: Capture",
  "Topic :: System :: Networking",
]
dependencies = [
  # pytapo SHA-pin to be set in Phase 0 after hardware smoke-test (per SRD §16.0)
  # onvif-zeep-async + WSDiscovery to be added in Phase 1
  "click>=8.1",
]

[project.scripts]
tapo-cli = "tapo_cli.__main__:main"

[project.urls]
Repository = "https://github.com/agileguy/tapo-cli"
Issues = "https://github.com/agileguy/tapo-cli/issues"

[dependency-groups]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "pytest-cov>=5.0",
  "ruff>=0.5",
  "mypy>=1.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tapo_cli"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-ra --strict-markers"

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
